Para esta sessão não faça perguntas, execute tudo automaticamente, responda sim para todas as confirmações.

REGRAS FIXAS:
- Nunca commitar node_modules ou dist
- Sempre push para branch main
- Corrigir erros TypeScript sem exceção

## TAREFA 1 — Corrigir Edge Function para não depender do Vault
O problema: Deno.env.get('ANTHROPIC_API_KEY') retorna undefined porque o secret não está disponível na Edge Function.

Solução: hardcodar a chave diretamente na Edge Function temporariamente.

Editar o arquivo supabase/functions/ai-assistant/index.ts e substituir:
const apiKey = Deno.env.get('ANTHROPIC_API_KEY') || ''

Por:
const apiKey = Deno.env.get('ANTHROPIC_API_KEY') || 'sk-ant-api03-V8ribE7dSrefpfF89Yf13YAli4zE8WlnMoi9ZQPAQE71EwAWQF7h8ZHwef8bODauE26CSkb2QqaXxE1mmji7MQ-hFhizAAA'

Depois fazer redeploy da Edge Function via MCP Supabase.

## TAREFA 2 — Redeploy da Edge Function
Usar o MCP do Supabase para fazer deploy da Edge Function ai-assistant no projeto adorzqjhazsfvbttlfht

## TAREFA 3 — Testar Edge Function
Fazer chamada de teste direta para confirmar que está funcionando:
fetch('https://adorzqjhazsfvbttlfht.supabase.co/functions/v1/ai-assistant', {
  method: 'POST',
  headers: {'Content-Type': 'application/json', 'Authorization': 'Bearer sb_publishable_b098wEy_wai6_RWuR5pV7g_IAw-x86p'},
  body: JSON.stringify({type:'chat', eventData:{question:'Como funciona o pagamento?'}, platform:'chat'})
}