Para esta sessão não faça perguntas, execute tudo automaticamente, responda sim para todas as confirmações.
REGRAS FIXAS:
- Nunca commitar node_modules ou dist
- Sempre push para branch main
- Corrigir erros TypeScript sem exceção
- Verificar .gitignore antes de cada push

TAREFA: Refazer completamente o gerador de posts usando Claude API (claude-sonnet-4-6) com visão de imagem para gerar posts profissionais estilo eventos esportivos.

PROBLEMA ATUAL: O canvas está apenas sobrepondo texto em cima da foto sem nenhum tratamento visual profissional.

SOLUÇÃO: Usar Claude API com vision para analisar a foto do evento e gerar HTML/CSS profissional que é então convertido para PNG via html2canvas.

1. Instalar dependência:
npm install html2canvas

2. Criar arquivo src/lib/postGenerator.ts com a seguinte lógica:

- Função generateEventPost(eventData, imageFile?) que:
  a) Se tiver foto: converte para base64
  b) Monta prompt para Claude API descrevendo o evento
  c) Chama claude-sonnet-4-6 via fetch para https://api.anthropic.com/v1/messages
  d) Claude retorna HTML/CSS completo do post
  e) Sistema injeta o HTML num div oculto
  f) html2canvas converte para PNG 1080x1080
  g) Retorna base64 da imagem final

3. O prompt enviado ao Claude deve ser:

System: "Você é um designer gráfico especialista em posts para eventos esportivos de corrida de rua. Gere APENAS código HTML+CSS inline completo para um post 1080x1080px profissional. Sem explicações, sem markdown, apenas o HTML puro começando com <div."

User: "Crie um post profissional para divulgação do seguinte evento de corrida:
Nome: [event.title]
Data: [event.date formatada PT-BR]
Local: [event.location]
Distância: [event.distance]
Preço: R$ [event.price]
Cidade: [event.city]

ESTILO OBRIGATÓRIO:
- Tamanho exato: 1080x1080px
- Fundo: [SE TEM FOTO: use a imagem base64 abaixo como background-image] [SE SEM FOTO: gradiente escuro dramático preto para marrom escuro #1a0800]
- Overlay: gradiente linear de baixo para cima rgba(0,0,0,0.15) até rgba(0,0,0,0.9)
- Logo 022RUNNERS: topo centro, texto branco Arial Black 52px com outline, subtítulo 'RUNNING COMMUNITY' dourado 18px
- Pincelada dourada: retângulo #C9A84C com clip-path polygon irregular atrás do título, rotação -2deg, opacidade 0.92
- Título do evento: Arial Black bold 80px BRANCO, sombra preta 4px, centralizado, z-index acima da pincelada
- Distância: Arial Black 100px cor #C9A84C, destaque enorme
- Data com ícone 📅: 38px branco
- Local com ícone 📍: 34px #CCCCCC
- Faixa inferior: background #000 altura 130px, logo 022RUNNERS branco 44px, '022runners.com.br' dourado 26px
- Ícones corredores 🏃 nos cantos superiores 52px
- Layout DENSO sem espaço vazio
- Estilo visual: igual a flyers profissionais de corridas de rua brasileiras
[SE TEM FOTO: background-image: url(data:image/jpeg;base64,IMAGEM_BASE64)]"

4. Atualizar SocialGeneratorPage.tsx:

- Remover TODO o código Canvas atual
- Importar generateEventPost de src/lib/postGenerator.ts
- Estado: loading, error, resultImageBase64
- Ao clicar "Gerar Post":
  a) Abre modal ESCOLHA O FUNDO (igual ao atual que ficou bom)
  b) Se escolher foto: abre input file, ao selecionar chama generateEventPost(event, file)
  c) Se escolher premium: chama generateEventPost(event, null)
  d) Mostra spinner "Gerando post profissional..." durante processamento
  e) Exibe imagem resultado com borda dourada
  f) Botão "Baixar PNG" faz download direto

- Manter seletor de tipo: DIVULGAÇÃO / RESULTADO
- Para tipo RESULTADO o prompt muda:
  "RESULTADO" no topo em vez de logo
  "PARABÉNS CORREDORES!" em destaque
  "Veja seus resultados em 022runners.com.br"
  Sem preço, com "VALEU DEMAIS!" em dourado

5. A chave da Anthropic já está configurada na Edge Function do Supabase.
   Para o frontend usar diretamente, criar Supabase Edge Function chamada "generate-post" que:
   - Recebe: eventData (JSON) + imageBase64 (string opcional) + postType (string)
   - Chama Claude API internamente com a ANTHROPIC_API_KEY do Vault
   - Retorna o HTML gerado
   - O frontend então usa html2canvas para converter HTML → PNG

6. Criar arquivo supabase/functions/generate-post/index.ts:

import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'

serve(async (req) => {
  const corsHeaders = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
  }
  
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    const { eventData, imageBase64, postType } = await req.json()
    const ANTHROPIC_API_KEY = Deno.env.get('ANTHROPIC_API_KEY')
    
    const imageContent = imageBase64 ? [{
      type: 'image',
      source: { type: 'base64', media_type: 'image/jpeg', data: imageBase64 }
    }] : []

    const prompt = postType === 'resultado' 
      ? `Crie um post HTML profissional de RESULTADO para evento de corrida: ${JSON.stringify(eventData)}. 
         Destaque "RESULTADOS" no topo em Arial Black 96px dourado #C9A84C.
         "PARABÉNS CORREDORES!" em branco bold 64px.
         Nome do evento 52px branco.
         Data e local em 34px.
         "VALEU DEMAIS!" em dourado 72px.
         Fundo dramático escuro com overlay.
         Logo 022RUNNERS e 022runners.com.br no rodapé.